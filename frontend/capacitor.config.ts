import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.technyks.watchPay',
  appName: 'Watch pay App',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
